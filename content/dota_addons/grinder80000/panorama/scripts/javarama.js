(function() {
    'use strict';

    var $$ = function(what) {
        return new Selector(what);
    };

    var Selector = function(what) {
        Panorama;
//        if
    };

    $.Msg('Javarama ready');
})();
